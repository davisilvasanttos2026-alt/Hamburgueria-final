# TODO - Combos no carrinho

- [ ] Criar combos promocionais na seção `#combos` do `index.html`.
- [ ] Adicionar botão “Adicionar ao Carrinho” em cada combo.
- [ ] Implementar JS `adicionarComboAoCarrinho(comboId)` que preenche o `select#lanche` e define `quantidade=1`, depois chama `atualizarTotal()`.
- [ ] Ajustar `fazerPedido()` para exibir corretamente o combo selecionado (título do item do `select#lanche`).
- [ ] Testar no navegador: clicar em combo altera total e finaliza pedido com o combo.

